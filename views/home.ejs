<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Hello <%= name %></h1>
    <p>Message displayed using the = sign in the ejs tags:</p>
    <%= message %>
    <p>Message displayed using the - sign in the ejs tags:</p>
    <%- message %>
    <!-- We can use logic by using the ejs tags without a sign -->
    <% if(showText){ %>
        <%= message %>
    <% } %>
    <!-- Loop through data -->
    <ul>
        <% for(let i=0; i < data.length; i++){ %>
            <li><%= data[i] %></li>
        <% } %>
    </ul>
</body>
</html>